FROM ubuntu:noble

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /bench
COPY bench_data.h bench_cache.c ./

RUN gcc -O2 -o bench_cache bench_cache.c

ENTRYPOINT ["./bench_cache"]
