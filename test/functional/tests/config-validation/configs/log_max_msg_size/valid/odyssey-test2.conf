log_format "%p %t %l [%i %s] (%c) %M\n"
log_max_msg_size 32768

listen {
        host "*"
}

storage "postgres_server" {
        type "remote"
        host "*"
}

database "db" {
        user "user" {
                storage "postgres_server"
                pool "session"
                authentication "none"
        }
}
