\connect 'host=localhost port=7432 user=postgres dbname=postgres'

select pg_is_in_recovery();

select pg_is_in_recovery();

select pg_is_in_recovery();

\connect 'host=localhost port=8432 user=postgres dbname=postgres'

select pg_is_in_recovery();

select pg_is_in_recovery();

select pg_is_in_recovery();

\connect 'host=localhost port=7432 user=postgres dbname=postgres'

select pg_is_in_recovery();

select pg_is_in_recovery();

select pg_is_in_recovery();

\connect 'host=localhost port=8432 user=postgres dbname=postgres'

select pg_is_in_recovery();

select pg_is_in_recovery();

select pg_is_in_recovery();
