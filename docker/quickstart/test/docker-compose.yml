services:
  primary:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_DB=postgres
    networks:
      - od_alpine_build
    healthcheck:
      test: 'pg_isready -U postgres --dbname=postgres'
      interval: 1s
      timeout: 1s
      retries: 3

  odyssey:
    image: odyssey
    volumes:
      - ./test.conf:/etc/odyssey/odyssey.conf
    networks:
      - od_alpine_build
    depends_on:
      - primary

  tester:
    build:
      context: ../../../ # odyssey root dir
      dockerfile: ./docker/quickstart/test/Dockerfile
    networks:
      - od_alpine_build
    depends_on:
      - odyssey

networks:
  od_alpine_build:
    driver: bridge
